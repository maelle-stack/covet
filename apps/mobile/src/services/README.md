# services

API clients and local device integrations (e.g. biometrics, secure storage,
push token registration). Talks to the backend defined in `apps/backend`;
never holds Plaid/calendar/AI provider secrets directly. Built in Phase 6.
