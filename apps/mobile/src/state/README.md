# state

Zustand stores for lightweight local/UI state only: active tab, onboarding
step, temporary form input, unsaved chat input. Server state (SafeToSpend
snapshot, Activity, Upcoming, Notifications, Settings) belongs in TanStack
Query, not here.
