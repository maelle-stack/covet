# components

Reusable, presentational UI components shared across screens (buttons,
cards, wallet visualization, status labels, etc.). Built in Phase 5 against
`@covet/design-tokens` and the founder's UI/UX references.
