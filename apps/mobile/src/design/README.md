# design

App-local design glue: motion constants, wallet state mappings, and any
mobile-specific wrappers around `@covet/design-tokens`. Token _values_ live
in the `@covet/design-tokens` package, not here.
