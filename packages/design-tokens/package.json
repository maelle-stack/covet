{
  "name": "@covet/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Design token placeholders for Covet. Values are populated once founder UI/UX references and fonts are added; do not treat current values as final.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
