{
  "name": "@covet/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and API contracts used by the Covet mobile app and backend services.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
